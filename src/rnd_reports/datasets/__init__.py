"""Датасеты для R&D-6: единая обёртка ``BenchmarkDataset`` + каталог/loaders/adapters.

Фундамент Step 1: контракты и заглушки. Реальные данные **не коммитятся** —
loaders читают локальные файлы из gitignored-директории (см. ``loaders.DEFAULT_DATA_DIR``).
"""
